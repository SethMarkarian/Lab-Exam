import java.awt.Point;
import java.util.*;
public interface Work
{
    void pointSet(); //assigns points to pts from arr
    ArrayList maximalTranslatablePattern(Point u);  //adds mtps to arraylist
}
